import { makeStyles } from '@material-ui/core';

const styles = makeStyles(theme => {
  return {
    NavbarTemplateRoot: {
      margin: 'auto',
      padding: '0',
    },
  };
});

export default styles;
