import React, { FunctionComponent } from 'react';
import Box from '../../atoms/Box';

interface OwnProps {
  
}

type Props = OwnProps;

const LangToggler: FunctionComponent<Props> = (props) => {
  
  return (
    <Box>

    </Box>
  );
};

export default LangToggler;
